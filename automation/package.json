{
  "name": "workflow-automation-playwright",
  "version": "1.0.0",
  "description": "Playwright automation scripts for provider portal simulation",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "scrape:demo": "npx ts-node scripts/provider-scraper.ts"
  },
  "dependencies": {
    "@playwright/test": "^1.40.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  }
}
